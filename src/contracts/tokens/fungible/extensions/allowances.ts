import { Extension } from "./extension";

const name    : string = `allowances`;
const state   : string = ``;
const types   : string = ``;
const init    : string = ``;
const methods : string = ``;

export default new Extension(name, state, types, init, methods);